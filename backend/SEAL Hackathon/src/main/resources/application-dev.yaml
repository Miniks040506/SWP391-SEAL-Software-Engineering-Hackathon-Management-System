# src/main/resources/application-dev.yml

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

logging:
  level:
    com.seal.hackathon: DEBUG
    org.springframework.web: DEBUG